import DS from 'ember-data';

export default DS.Model.extend({
  name: DS.attr('street'),
  name: DS.attr('city'),
  name: DS.attr('zipCode'),
  name: DS.attr('state'),
  name: DS.attr('country')
});
