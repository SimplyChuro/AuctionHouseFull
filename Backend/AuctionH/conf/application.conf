#PersonalCode
	
	ebean.default = ["models.*"]
	
	db.default.driver=org.postgresql.Driver
	db.default.url="jdbc:postgresql://localhost/AuctionTestDB"
	db.default.user=postgres
	db.default.password="retro123"
	db.default.logStatements=true
	
	play.filters.enabled += "play.filters.cors.CORSFilter"
	
	
	play.mailer {
	    host="smtp.gmail.com"
		port=587
		ssl=no
  		tls=yes
		user="auctionhouse.atlantbh@gmail.com"
		password="mrvrjbkgthlfkimv"
		debug=no
	    mock=false
	}
	

play.filters {

  cors {
    # Filter paths by a whitelist of path prefixes
    #pathPrefixes = ["/some/path", ...]

    allowedOrigins = ["http://localhost:4200", "https://auction-house-ah.herokuapp.com"]
    allowedHttpMethods = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
  }

  hosts {
    allowed = ["localhost:9000", "auction-house-backend.herokuapp.com"]
  }
  
  csrf.header.bypassHeaders {
	  X-Requested-With = "*"
	  Csrf-Token = "61ZwHIHbEjSABToXNQLcDZtH3TtlOUKn"
	}
  
}


